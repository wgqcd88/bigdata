{{- if .Values.thriftServer.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: spark-thrift-server
  namespace: {{ include "spark.namespace" . }}
  labels:
    {{- include "spark.thrift.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "spark.thrift.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "spark.thrift.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.thriftServer.ports.driverUi }}"
        prometheus.io/path: "/metrics/prometheus/"
    spec:
      securityContext:
        runAsUser: 185
        runAsGroup: 0
        fsGroup: 0
      {{- with .Values.thriftServer.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ .Values.serviceAccount.name }}
      containers:
        - name: spark-thrift-server
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["/opt/spark/sbin/start-thriftserver.sh"]
          env:
            - name: SPARK_USER
              value: spark
            - name: TZ
              value: {{ .Values.timezone | quote }}
            - name: SPARK_NO_DAEMONIZE
              value: "1"
            - name: APP_NAME
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: metadata.name
            - name: SPARK_DRIVER_BIND_ADDRESS
              valueFrom:
                fieldRef:
                  apiVersion: v1
                  fieldPath: status.podIP
          ports:
            - name: spark-thrift
              containerPort: {{ .Values.thriftServer.ports.thrift }}
            - name: driver-ui
              containerPort: {{ .Values.thriftServer.ports.driverUi }}
            - name: driver-rpc
              containerPort: {{ .Values.thriftServer.ports.driverRpc }}
            - name: block-manager
              containerPort: {{ .Values.thriftServer.ports.blockManager }}
          livenessProbe:
            tcpSocket:
              port: {{ .Values.thriftServer.ports.thrift }}
            initialDelaySeconds: 60
            periodSeconds: 30
          readinessProbe:
            tcpSocket:
              port: {{ .Values.thriftServer.ports.thrift }}
            initialDelaySeconds: 30
            periodSeconds: 10
          {{- with .Values.thriftServer.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            - mountPath: "/opt/spark/conf"
              name: spark-thrift-server-volume
      volumes:
        - name: spark-thrift-server-volume
          configMap:
            name: spark-thrift-defaults
{{- end }}
